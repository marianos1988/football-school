export type PropertiesHome = {
    properties : {
        blur: boolean,
        navLayer: boolean,
        section: string,
        btnBurguer: boolean,
        stateSpinner: boolean,
        stateConfirmationPoster: boolean
    }
}